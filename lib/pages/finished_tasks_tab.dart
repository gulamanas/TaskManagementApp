import 'package:flutter/material.dart';

class FinishedTasksTab extends StatelessWidget {
  const FinishedTasksTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Finished Tasks'),
    );
  }
}